<tr>
  <td><%= route.title %></td>
  <td><%= link_to t('common.show'), [:admin, route] %></td>
  <td><%= link_to t('common.edit'), edit_admin_route_path(route) %></td>
  <td><%= link_to t('common.destroy'),
                  [:admin, route],
                  method: :delete,
                  data: { confirm: t('common.sure') } %></td>
</tr>
